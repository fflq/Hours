<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>


    </style>

    <style name="Widget.MaterialComponents.CardView.Hours" parent="Widget.MaterialComponents.CardView">
        <item name="android:layout_marginStart">12dp</item>
        <item name="android:layout_marginEnd">12dp</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="cardCornerRadius">@dimen/card_corner_radius</item>
        <item name="contentPadding">0dp</item>
    </style>

    <style name="ThemeOverlay.aa" parent="">

    </style>

    <style name="Widget.MaterialComponents.CardView.Hours.Padding" parent="Widget.MaterialComponents.CardView">
        <item name="android:layout_marginStart">12dp</item>
        <item name="android:layout_marginEnd">12dp</item>
        <item name="android:layout_marginTop">12dp</item>
        <item name="cardElevation">@dimen/card_elevation</item>
        <item name="cardCornerRadius">@dimen/card_corner_radius</item>
        <item name="contentPadding">4dp</item>
    </style>

    <declare-styleable name="PomodoroView">
        <attr name="circle_color" format="color"/>
        <attr name="arc_color" format="color"/>
        <attr name="stroke_width" format="integer"/>
    </declare-styleable>

</resources>
