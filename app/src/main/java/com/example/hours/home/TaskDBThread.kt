package com.example.hours.home

import android.app.Activity

class TaskDBThread(var activity: Activity) {
    companion object {
        fun updateToday() {
            //var taskViewModel = ViewModelProvider(this)[TaskViewModel::class.java]
        }
    }
}